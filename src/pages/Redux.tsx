import PageTitle from "../components/PageTitle";
import ReduxList from "../components/ReduxList";

const Redux = () => {
  return (
    <div id="Redux" style={{ marginTop: "2em" }}>
      <PageTitle text="Redux Testing" />
      <ReduxList />
    </div>
  );
};

export default Redux;
